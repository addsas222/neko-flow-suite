# Generated by neko-plugin
# managed-template: plugin-market-actions-v1
target-version = "py311"
line-length = 120
# vendor = 第三方搬运代码；stubs = MaiBot SDK 缺位时的本地替身，都不参与套件 lint。
extend-exclude = ["vendor", "stubs"]
respect-gitignore = false

[lint]
select = ["E4", "E7", "E9", "F", "I"]

