{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "N.E.K.O: check flow_evomap",
      "type": "shell",
      "command": "uv run --with pip neko-plugin check flow_evomap",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    },
    {
      "label": "N.E.K.O: sync flow_evomap",
      "type": "shell",
      "command": "uv run --with pip neko-plugin sync flow_evomap --clean",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    },
    {
      "label": "N.E.K.O: build flow_evomap",
      "type": "shell",
      "command": "uv run --with pip neko-plugin build flow_evomap",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    }
  ]
}
